---
description: SwiftUI 与莫兰迪设计规范（Bonne-Santé iOS 视图）
globs: CalorieCop/**/*.swift
alwaysApply: false
---

# SwiftUI 设计规范

## 视觉

- 字体：系统 San Francisco + Dynamic Type
- 图标：仅 SF Symbols 5
- 颜色：使用 `Theme.swift`，禁止硬编码系统 `.blue`
  - 主色 `#ADC8F5`、背景 `#F9FBFF`、强调 `#E5A5CF`、警示 `#E8A0A0`
- 卡片：`.regularMaterial`，圆角 16pt，无硬阴影
- 间距：4 倍数网格，水平边距 20pt

## 组件

- 热量进度：`CircularProgress`
- 周期展示：`PhaseBar`
- 每个新 View 必须包含 `#Preview` + 模拟数据

## 导航

5 Tab：首页（仪表盘）、健康（档案）、减脂（体态）、待办（提醒）、我的（设置）。

## 空状态

每 Tab 需 Empty State：大号 SF Symbol + 引导文案 + CTA。

## 首页信息层级

热量环为主视觉（约 40%）；滑动卡片默认最多 2 张可见。
